Simple URI scheme for requesting data from the fetch service.

Used with an authority (//), the market can be explicitly set
exchangedata://mtgox/market/BTC/USD

Without an authority (/), the default market is queried
exchangedata:/market/BTC/USD
